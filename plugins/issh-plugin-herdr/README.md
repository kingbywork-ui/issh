# issh-plugin-herdr

issh 桌面客户端（Tauri 版）的 Herdr/Workspace 插件。

## 功能

- 工作区创建与列表（isshd `workspace.create` / `workspace.list`）
- 终端会话绑定/解绑（`workspace.bind` / `workspace.unbind`）
- Runtime health 与 workspace 能力展示

## 开发

```bash
npm install
npm run build
npm run package
```
